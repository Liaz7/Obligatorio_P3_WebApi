@{
    ViewData["Title"] = "Subir foto";
}

@if (Context.Session.GetString("user") == null)
{
    <div class="alert alert-danger" role="alert">
        Accesso Denegado!
    </div>
}
else
{
    


<h1>Display</h1>

@using (Html.BeginForm("FotoDEspecie", "Especie", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input type="file" name="ImageFile" />
    <input type="submit" value="Upload" />
    //<input type = "hidden" name = "EsNombreCientifico" value = "@ViewBag.EsNombreCientifico" />
}
}